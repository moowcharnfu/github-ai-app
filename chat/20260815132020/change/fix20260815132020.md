# 本轮修复记录

## 修复范围
1. `src/utils/api.js`：增加模型配置校验；统一 timeout/abort 的异常映射；非流式响应空体降级返回空串，避免 `JSON.parse(empty)` 崩溃。
2. `src/components/ChatArea.vue`：统一用 `showError` 替代 `alert`；图片发送时保留 `[图片]` 占位文本；失败时恢复草稿；完成/错后完成让输入框归位。
3. `src/components/ConfigModal.vue`：弹窗支持响应式 `min`/`max`；小屏隐藏左侧列表；删除按钮改为二次确认。
4. `src/components/ToolBar.vue`：下拉框增加高度上限、内部滚动；支持根据窗口空间选择向上或向下展开。
