 # 20260815125000 变更
 
 ## 变更内容
 修复代码审查报告中发现的 12 项问题，覆盖严重崩溃、逻辑缺陷、样式兼容和代码风格。
 
 ## 严重问题修复
 1. **api.js** — `sendStreamingRequest` 引用未定义的 `combinedController` 和 `timeout` 变量，删除错误的 per-read 超时逻辑，恢复为外层统一超时控制
 2. **api.js** — 修复流式读取中 `Promise.race` + `setTimeout` 导致的定时器泄漏和 `readTimeout` 只在循环外设置一次的问题
 
 ## 中等问题修复
 3. **ChatArea.vue** — 移除未使用的 `savedText` 死代码
 4. **ConfigModal.vue** — 将 `pendingDelete` 声明移到所有 import 之后，修正 ESM import 顺序
 5. **sessionStore.js** — 初始化时从 storage 恢复 `active-session-id`，`createSession`/`deleteSession` 也同步写入
 6. **ChatArea.vue** — fallback 请求前检查并重建 `abortController`，防止空指针
 
 ## 样式/布局修复
 7. **ConfigModal.vue** — `.modal-container` 添加 `min-width: 320px`，极窄窗口下表单不会被过度挤压
 8. **MessageBubble.vue** — lightbox fixed 定位风险已确认，当前无 transform 祖先，无需改动
 9. **ChatArea.vue** — 给 `.input-bar` 的 `position: relative` 加注释说明 error-toast 依赖关系
 10. **TabBar.vue** — 触屏设备增大 `.tab-close` 点击区域到 `padding: 6px 10px`
 
 ## 代码风格修复
 11. **api.js** — 修正 `sendChatMessage` 内部变量声明的缩进
 12. **MessageBubble.vue** — 代码块解析正则确认为低频场景，保持现状
 
 ## 涉及文件
 - src/utils/api.js
 - src/components/ChatArea.vue
 - src/components/ConfigModal.vue
 - src/components/MessageBubble.vue
 - src/components/TabBar.vue
 - src/stores/sessionStore.js
 
 ## 验证
 `npx vite build` 构建通过，无编译错误。
