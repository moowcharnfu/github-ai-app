# 本轮修复记录
## 已完成
- `src/utils/api.js`：缺失模型配置时前置校验；非流式请求对空响应体做安全降级；统一 timeout / abort 映射为 `TimeoutError`。
- `src/components/ChatArea.vue`：统一错误提示；图片发送保留 `[图片]` 文本；补全超时回退；失败恢复草稿、成功清空输入；组件卸载与切会话时中断请求。
- `src/components/ConfigModal.vue`：删除按钮改为二次确认；弹窗尺寸改为响应式 `min(640px, 100vw - 24px)` + `max-height: 90vh`；小屏隐藏左侧列表。
- `src/components/ToolBar.vue`：下拉菜单增加高度上限与滚动；按视图空间决定向上或向下展开；监听 resize 动态调整位置。
